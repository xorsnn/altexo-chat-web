md-toolbar.md-hue-2(hide-gt-sm='')
  .md-toolbar-tools
    md-button.md-icon-button(aria-label='Settings', ng-click='leftSidenav.toggle()')
      md-icon(md-svg-icon="${require('../../../img/icons/ic_menu_black_24px.svg')}")
    h2(flex='', md-truncate='') Create room

md-content(layout='column', layout-padding='', layout-align='center center', flex='grow')
  form
    md-card(layout='column', md-whiteframe='4')
      md-card-header
        md-card-header-text
          span.md-title Create chat room and invite your friends
          span.md-subhead(style={'text-align': 'right', 'padding': '1em 0'})
            | Free one-to-one 3D video and holographic calls.
            | <br>No login required &mdash; no installs.
      md-card-content(layout-xs='column', layout='row', style={width: '25vw'})
        md-input-container(flex, style={margin:0})
          label Enter room name
          input(ng-model='room.name', required='')
          md-tooltip(md-direction='top', md-visible='tooltipVisible') Enter your cool room name here, or use automatically generated.
        //- md-checkbox(ng-model='room.kurento', ng-change="kurentoClick(room.kurento)")
        //-   | Kurento
      md-card-footer(layout='row', layout-align='end center')
        md-button.md-raised(ng-click='createRoom(chat, room)', style='height: 36px;') Create
